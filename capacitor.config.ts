import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.billete.verificador',
  appName: 'Verifica tu billetito',
  webDir: 'www'
};

export default config;
